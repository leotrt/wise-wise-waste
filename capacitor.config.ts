import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.leotrt.wisewisewaste',
  appName: 'Wise Wise Waste',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
